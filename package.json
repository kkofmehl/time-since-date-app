{
  "name": "time-since-date-app",
  "version": "1.0.0",
  "private": true,
  "description": "Web app for time-since / time-until timers with JSON persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
